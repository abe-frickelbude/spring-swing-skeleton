
<configuration>

	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<withJansi>false</withJansi>
		<!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
		<encoder>
			<pattern>%cyan(%d{HH:mm:ss.SSS}) %yellow([%thread]) %highlight(%-5level) %magenta(%logger{36}) - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Suppress excessive chatter from some components, e.g. Wiremock -->
	<logger name="org.eclipse.jetty" level="WARN" />
	<logger name="io.micronaut.context" level="WARN" />
	<logger name="io.micronaut.core" level="WARN" />

	<root level="info">
		<appender-ref ref="console" />
	</root>
</configuration>
